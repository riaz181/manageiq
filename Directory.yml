  - name: Create A Directory in DevBoxVM
    hosts: localhost
    vars:
    ansible_python_interpreter: /usr/bin/python3
    connection: local
    tasks:
    - name: Create Directory in VM
      azure_rm_virtualmachine:
        resource_group: "MngIQRg"
        name: DevBoxVM
        admin_username: azureuser
        admin_password: Pakistan@1947
        started: yes		
	  file:
      path: /home/devops_dir
      state: directory
      mode: "u=rw,g=wx,o=rwx"
